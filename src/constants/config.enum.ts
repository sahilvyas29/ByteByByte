export enum NODE_ENV {
    DEVELOPMENT = 'development',
    PRODUCTION = 'production',
}

export enum LogLevel {
    DEBUG = 'debug',
    INFO = 'info',
    WARN = 'warn',
    ERROR = 'error',
    HTTP = 'http',
}
